# Project Structure

- we decided the modular monolith architecuture first
- we will add microservices later
- we will created domain specific modules to make microservices transition easier from monolith
- we are using nest js for creating our backend as it has many useful features to not only code but to code production based architecture
- we are using postgres as database as we need relationships to create our project and estanlish relations between them we neede a sql database so i chosed postgres because of it usability
