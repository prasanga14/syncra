# Right now it contains:

 - Organization
 - Workspace
 - Membership
 - User

# including the discussions we had about:

 - why Membership doesn't need organization_id
 - why roles are workspace-scoped
 - relationship diagrams

We'll update it every time the schema changes.